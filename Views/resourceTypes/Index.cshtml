@model IEnumerable<allpax_service_record.Models.tbl_resourceTypes>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.resourceType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.resourceType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.resourceTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.resourceTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.resourceTypeID })
        </td>
    </tr>
}

</table>
