@model IEnumerable<allpax_service_record.Models.tbl_dailyReportTimeEntry>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tbl_dailyReport.jobID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.workDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_dailyReport.jobID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.workDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.timeEntryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.timeEntryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.timeEntryID })
        </td>
    </tr>
}

</table>
